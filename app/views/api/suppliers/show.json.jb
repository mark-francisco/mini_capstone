  # :locals => { "whenever you see:" [variable name used in the partial] => "you should actually use:" [variable name that gets passed to show.json.jb] }
  render :partial => "supplier.json.jb", :locals => { :eggs => @supplier }
  